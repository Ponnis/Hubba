<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nils_martin.hubba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".CalendarController"/>
        <activity android:name="MainActivityController">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".LoginView"/>

        <activity android:name=".AddHabitController" />
        <activity android:name=".CreateUser" />
        <activity android:name=".HabitView"/>
        <activity android:name=".MenuVM"/>
        <activity android:name=".SettingsVM"/>
        <activity android:name=".ProfileVM"/>
        <activity android:name=".MenuGroupsVM"/>
        <activity android:name=".MenuHabitsVM"/>
    </application>


</manifest>